import { api, loadBrand } from './api.js'; import { confirmAction, runAction, showResult } from './alerts.js';
document.head.insertAdjacentHTML('beforeend','<link rel="stylesheet" href="./assets/css/auth.css">');loadBrand();const form=document.querySelector('#reset-form'),emailInput=document.querySelector('#email');emailInput.value=new URLSearchParams(location.search).get('email')||'';
document.querySelectorAll('.password-toggle').forEach(button=>button.onclick=()=>{const input=document.getElementById(button.dataset.target);input.type=input.type==='password'?'text':'password'});
form.onsubmit=async event=>{event.preventDefault();if(!form.checkValidity())return form.reportValidity();const otp=document.querySelector('#otp').value.replace(/\D/g,''),password=document.querySelector('#password').value,confirmation=document.querySelector('#confirmation').value;if(otp.length!==6)return showResult({success:false,title:'Invalid Code',message:'Enter the complete 6-digit verification code.'});if(password!==confirmation)return showResult({success:false,title:'Password Mismatch',message:'The passwords do not match.'});if(!await confirmAction({title:'Reset Password?',text:'Your existing password will stop working immediately.',confirmText:'Reset Password',icon:'warning'}))return;try{await runAction(()=>api('/reset-password',{method:'POST',body:JSON.stringify({email:emailInput.value.trim(),otp,password,password_confirmation:confirmation})}),{loadingTitle:'Resetting password...',successTitle:'Password Reset'});location.replace('./login.html?reset=1')}catch{}};
