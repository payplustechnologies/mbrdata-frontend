import { api, loadBrand } from './api.js';
import { confirmAction, runAction, showResult } from './alerts.js';
document.head.insertAdjacentHTML('beforeend','<link rel="stylesheet" href="./assets/css/auth.css">'); loadBrand();
const form=document.querySelector('#register-form'),refInput=document.querySelector('#ref');refInput.value=new URLSearchParams(location.search).get('ref')||'';
document.querySelectorAll('.password-toggle').forEach(button=>button.onclick=()=>{const input=document.getElementById(button.dataset.target);input.type=input.type==='password'?'text':'password';button.querySelector('i').className=input.type==='password'?'bi bi-eye':'bi bi-eye-slash'});
form.onsubmit=async event=>{event.preventDefault();if(!form.checkValidity())return form.reportValidity();const phone=document.querySelector('#phone').value.replace(/\D/g,''),pin=document.querySelector('#pin').value.replace(/\D/g,'');if(phone.length!==11)return showResult({success:false,title:'Invalid Phone Number',message:'Phone number must contain exactly 11 digits.'});if(pin.length!==4)return showResult({success:false,title:'Invalid PIN',message:'Transaction PIN must contain exactly 4 digits.'});if(!await confirmAction({title:'Create Your Account?',text:'Please confirm that your registration details are correct.',confirmText:'Create Account'}))return;const button=form.querySelector('[type="submit"]');button.disabled=true;try{await runAction(()=>api('/register',{method:'POST',body:JSON.stringify({name:document.querySelector('#name').value.trim(),username:document.querySelector('#username').value.trim(),email:document.querySelector('#email').value.trim(),phone,ref:refInput.value.trim(),password:document.querySelector('#password').value,pin})}),{loadingTitle:'Creating your account...',loadingText:'Setting up your secure wallet.',successTitle:'Account Created'});location.replace('./login.html?registered=1')}catch{}finally{button.disabled=false}};
